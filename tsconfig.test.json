// Type-checks everything the build config emits, plus the test files the build
// deliberately leaves out of dist/. Emits nothing — `npm run build` owns emit.
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src/**/*"],
  "exclude": ["node_modules", "dist"]
}
