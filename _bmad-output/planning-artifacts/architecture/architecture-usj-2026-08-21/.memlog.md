---
scope: USJ 通行證庫存看板 — 抓取管線 + 靜態站台，涵蓋 PRD P0（法遵護欄）至 P3（廣告）全產品
purpose: build-substrate
altitude: feature
updated: 2026-08-21T22:06
---

- (direction) 工作模式：教練模式。脊椎範圍：整個產品 P0–P3。交付物：只要 ARCHITECTURE-SPINE.md，不另出人類向文件。
- (constraint) [PRD NFR1] 系統唯讀：不得建立購物車、保留庫存、自動下單或預約、涉入轉售。PRD 稱此為最強單一辯護事實，任何推向代訂/搶位/收費解鎖的功能提案都須先推翻此條。
- (constraint) [PRD NFR2] 不得從 Queue-it (nbcuniversal.queue-it.net) 後方取得資料，亦不得繞過/預熱/自動化通過候位機制。硬約束。
- (constraint) [PRD NFR3.1] 不得使用官方圖片，尤其不得熱連結官方圖片 URL — 那是呈現端唯一不受 src/limiter.ts 管轄、且與使用者流量成正比的對外請求通道。
- (constraint) [PRD NFR3.2] 站名、網域、品牌識別不得以 USJ/Universal/ユニバーサル 作為主要識別，不得用官方 logo 與識別色。
- (constraint) [PRD NFR4] 對來源主機的持續請求速率須有明確上限，且在程式碼中以單一常數集中控制。目標值 1 req/s、並行度 2（並行度降半有真實代價：冷啟動 12 分 → 約 21 分，逼近 timeout 25 分）。
- (constraint) [PRD NFR5.1] 同一時間只允許一個抓取回合執行。現況缺少保護（GitHub Actions 無 concurrency group）。
- (constraint) [PRD NFR9/9.1/9.2] 須有可立即停用抓取的 kill switch（當日生效），須定義停抓之後的完整程序（歷史資料處置/頁面是否下架/對外窗口），且頁面須有可運作的對外聯絡信箱。
- (constraint) [PRD FR16/NFR19] 核心內容（票種名稱、日期、可購狀態、剩餘量）須在不執行 JavaScript 的前提下可讀。現行 index.html 為用戶端渲染，直接衝突。
- (constraint) [PRD FR13/FR14/FR17] 每種語言須有獨立可索引 URL，互相宣告 hreflang 與 canonical，並產出涵蓋所有語言的 sitemap.xml。
- (constraint) [PRD FR3.1 / O1] 來源 API 只回傳可購日期，售罄日期直接不出現；資料中不存在售罄列（928 筆日期列全為 available:true）。系統不得把「售罄/尚未開賣/不營業」三者一律呈現為售罄。此為 P2 開工前置條件。
- (constraint) [PRD R15] 準據法為日本法，爭議走大阪 JCAA 日語仲裁；但 USJ 就智財與營業損失聲請假處分不受仲裁拘束 — 第一擊更可能是速度快、目的為關站的假處分。營運主體與託管選擇一旦上線很難改。
- (constraint) [PRD R14] 資料源可能在毫無預警下單方消失（對方改 schema/後端）。P0 的降速/kill switch/聲明對此完全無效，只能靠工程韌性與降級呈現（NFR11）。
- (event) 現況勘查完成：src/ 為 TypeScript 抓取管線（fetcher/sources.usj/limiter/dates/types），data/ 由 GitHub Actions 每 30 分 commit 回 repo，index.html 為 1372 行單檔 vanilla JS SPA（44KB inline script、用戶端 fetch data/、html lang 寫死 zh-TW），i18n/ 為廠商字串詞表。**無任何部署管線**：無 Pages workflow、無 CNAME、無 robots.txt、無 sitemap — 站台目前不存在於網路上。
- (event) 教練模式開始，第一題：抓取與站台的邊界。
- (decision) 抓取與站台解耦為兩條 workflow：fetcher 寫 data/ → workflow_run 觸發 build/deploy。Binds: 全部。Prevents: 站台的可部署性被抓取的成功與否綁架 — 抓取全滅時仍能重新部署、仍能改站上文案（NFR11、R14 要的韌性）。權衡過 A（單一 job，簡單但 NFR11 表達不乾淨、改版面得重抓）與 C（SSR，為不存在的即時性付代價，且把整站下架綁到第三方 runtime 後台）。註：必須用 workflow_run，GITHUB_TOKEN 推的 commit 不觸發 push workflow。
- (decision) 頁面產生器參數化：渲染核心以 (locale, 視角, 鍵) 為參數產出頁面，不得為個別頁面寫死渲染路徑。Binds: 站台建置。Prevents: O11（3 個 URL vs 約 1449 種長尾意圖）三個月後回來要求擴 URL 覆蓋時，架構已把便宜的路堵死。權衡過 G2（現在就出約 90 頁）— 否決，薄資料頁數量暴增會推高 R2 的 AdSense 資格風險，等於拿 P2.9 過審去賭 O11。
- (decision) 兩個閱讀主軸（FR2 依日期／依票種）各自擁有獨立 URL，皆於建置時產出；切換即導航，JS 只在可用時升級為無重載切換。Binds: FR2, FR13, FR16, NFR19。Prevents: 第二軸淪為只存在於 JS 環境的狀態 — 對爬蟲不存在、不可分享、不可索引。零額外抓取成本：days.json（日期優先）與 products/*.json（票種優先）兩份資料 fetcher 本來就都在寫。
- (constraint) 上兩項相乘的實際頁數：3 語 × 2 視角 = 6 個可索引 URL（非 3）。hreflang 矩陣須以「同一視角的三語互指」建立，不得跨視角互指。
- (version) 查證 2026-08-21：Astro 最新穩定 7.2.x（7.0 於 2026-06-04 發佈，建置改 Rust）；Eleventy 最新穩定 3.1.6（4.0 仍為 alpha）。
- (version) 查證 2026-08-21（Cloudflare Pages 官方 limits 文件）：免費方案 500 builds/月、同時 1 個建置、建置逾時 20 分、20,000 檔/站、單檔 25 MiB、100 自訂網域。文件未區分 Direct Upload 與 Git 建置。
- (version) 查證 2026-08-21（GitHub Pages 官方 limits 文件）：repo 建議上限 1GB、已發佈站台上限 1GB、頻寬軟上限 100GB/月、建置軟上限 10 次/小時 —— 但『若以自訂 GitHub Actions workflow 建置與發佈則此限不適用』。免費方案僅 public repo 可用 Pages（Pro 才支援私有 repo）。
- (decision) 託管採 GitHub Pages，否決 Cloudflare Pages。Binds: 部署。Prevents: 選一個部署額度與資料新鮮度目標直接衝突的平台 —— 每 30 分部署 ≈ 1,440 次/月，是 Cloudflare 免費額度 500 次/月的 3 倍；壓進額度需降到約每 1.5 小時，會打破 §3『庫存摘要新鮮度 ≤ 1 小時』。GitHub Pages 走自訂 Actions workflow 明文豁免 10 次/小時的軟上限。
- (decision) 抓取資料與其歷史不得進入任何公開物件。實作形態：資料 repo 私有（承載 src/ 抓取管線、data/ 與其完整 git 歷史），站台 repo 公開且只收建置產物，供 GitHub Pages 發佈。Binds: 全部。Prevents: NFR9.1 第 1 項（停抓後銷毀抓取資料及衍生物）因歷史已被 fork/GHArchive/clone 散佈而實質不可執行；同時保住 §11 補貨訊號所需的歷史累積。代價：跨 repo 觸發需一個 fine-grained PAT。
- (constraint) 上項與『兩條 workflow 以 workflow_run 解耦』的相容性：workflow_run 只在同一 repo 內有效。因此 fetch 與 build 兩條 workflow 皆留在私有資料 repo（fetch → workflow_run → build），由 build 以 PAT 將產物推入公開站台 repo。跨 repo 的是產物推送，不是觸發。
- (question) 公開站台 repo 的發佈方式二選一：(a) Pages 直接從分支發佈 —— 無須 workflow，但受 10 次/小時軟上限（目前 2 次/小時，餘裕足但無豁免）；(b) 站台 repo 內放一支 actions/deploy-pages workflow —— 多一個零件，但明文豁免頻率上限。留待 Finalize 前決定。
- (decision) 建置工具：手寫 Node/TypeScript 頁面產生器，與現有 src/ 共用同一支 tsc，不引入 SSG 框架。Binds: 站台建置。Prevents: 為六個頁面付出整個框架生態的依賴與升級面；以及 index.html 現有渲染邏輯（純函式式字串組裝）被迫全部重寫。R14 的延伸考量：依賴面小的專案在低度維護半年後仍 build 得起來。權衡過 Astro 7.2.x（i18n routing/sitemap 現成，但 index.html 全重寫、依賴由 3 個變數百個）與 Eleventy 3.1.6（只換到模板引擎，i18n/hreflang 仍要自己接）。
- (decision) 手寫產生器的配套（S3 的成立條件，非可選）：須有一支建置後 SEO 驗證腳本，檢查每頁具 canonical、三語 hreflang 三向互指且對稱、sitemap 涵蓋所有頁、html lang 與內容語言一致。Binds: FR13, FR14, FR15, FR17。Prevents: hreflang/canonical/sitemap 手寫錯誤 —— 這類錯誤是靜默失敗，不報錯、只是沒有流量。既有 src/i18n-check.ts 是同型別的前例。
- (decision) 站台不做任何 runtime 資料 fetch，頁面所需事實全部於建置時 bake 進 HTML。Binds: FR3, FR5, FR16, NFR19。Prevents: 核心內容退回成需要 JS 才讀得到。成立依據：FR3 需要的 units 與 FR5 需要的 slots 數量 days.json 皆已有，且 FR5 明文不展開逐時段明細 —— products/*.json 的 timeSlots 完全不需要進站台。
- (decision) 公開站台不發佈任何原始資料 JSON（只發佈渲染後的 HTML 與 FR21 的逐頁 JSON-LD）。Binds: 站台建置, R13。Prevents: 把 days.json 掛成任何人可 curl 的免費 API —— 那會同時餵養兩家收費競品，並最大化 R13『無償、公開、對抗性地散布資料源刻意不揭露的商業價值』這個行為態樣評價。
- (decision) FR3.1 的三態（售罄／尚未開賣／不營業）只在 fetcher 推導一次，寫成 days.json 的顯式欄位；產生器與任何下游消費者一律讀取，不得自行重推。Binds: FR3.1, O1, §2 目標三。Prevents: 多個消費者（每票種頁、JSON-LD、§11 補貨分析）各自重推而互相分歧。技術依據：『該日是否所有票種皆無』需要跨票種視野，而 fetcher 是唯一同時看得到所有票種的地方（它已在做轉置產生 days.json）。
- (decision) 三態降級：latestDate 若無法穩定切分，該格呈現為顯式的『未知／無資料』並連向官方頁，絕不猜測。Binds: FR3.1, NFR11, NFR15.1。Prevents: 把『尚未開賣』誤報為『售罄』——那會側錄使用者做出『看到售罄就不計畫了』的錯誤決定，與 NFR15.1『一切以 USJ 官方頁面為準』直接牴觸。
- (decision) days.json 因三態欄位加入而升版（schemaVersion 1 → 2）；產生器遇到不認識的 schemaVersion 必須中止建置並報錯，不得降級渲染。Binds: 資料契約。Prevents: fetcher 與產生器對同一份檔案的理解無聲分歧，渲染出看似正常但語意錯誤的頁面。
- (decision) kill switch 為三級（L1 停抓／L2 停抓+站台明示停止服務／L3 整站下架），每一級皆須由單一動作達成且不依賴任何第三方後台。主機制為 repo 內檔案旗標（存在即 exit 0，git 留稽核紀錄，且建置讀得到 —— 站台會自動掛上 L2 聲明），GitHub UI 停用 workflow 為後備。Binds: NFR9, NFR9.1, NFR19, R15。Prevents: 抓取已停而站台不知情，繼續以正常樣式顯示逐漸腐朽的資料；以及 R15 的假處分情境下 L3 需要登入某個第三方後台才做得到。
- (decision) 節制參數的上限以測試斷言（Node 20 內建 node:test，零新依賴）：RATE_LIMIT_PER_SEC <= 1、CONCURRENCY <= 2、MAX_REQUESTS_PER_RUN <= 6000。Binds: NFR4, NFR5, R3.2。Prevents: PRD §7.2 第 3 項風險 —— 一次順手優化讓整套法遵判斷失效。註解已被證明擋不住（limiter.ts 的警告註解此刻就在，值仍是 5）。附帶價值：在 R3.2（使用条件禁止造成不当な負担や負荷）下，這是一份可稽核、機器強制的自我節制紀錄。此 repo 目前無任何測試，這是第一支。
- (constraint) 五值互鎖：RATE_LIMIT_PER_SEC、CONCURRENCY、cron 間隔、timeout-minutes、STALE_MS 互相牽動，不得單獨調整其中之一而不重算其餘。現況為孤立單值變更（5 → 1 req/s，冷啟動 10.5 → 約 12 分，仍遠低於 timeout 25）；但一旦 CONCURRENCY 由 4 降到 2，冷啟動推到約 21 分並逼近 timeout，五值須整組重算。
- (decision) 頁面文案（chrome strings）由 index.html 的 STRINGS 遷入 i18n/ui.<locale>.json，與既有 i18n/terms.<locale>.json 同構；key 完整性由擴充後的 src/i18n-check.ts 在 CI 檢查。Binds: FR10, FR12。Prevents: localizing skill 已明文警告的 undefined 陷阱（缺一個 key 就 render undefined，該表全有全無），以及新增語言時「一半改 JSON、一半改程式碼」的不對稱。
- (constraint) 上項的連帶動作：.claude/skills/localizing/SKILL.md 必須同步更新 —— 它現在明文寫著 chrome strings 住在 index.html 的 STRINGS。脊椎改了而 skill 沒改，下一個做在地化的人或 agent 會照著錯的地圖走。
- (decision) 告警＝定義失敗邊界，不新增通知管道。以下五種情況必須讓 job 失敗（靠 GitHub 內建失敗通知）：被來源封鎖（403 或連續 429）、budgetExhausted 觸發、連續 N 回合抓取失敗、資料齡超過門檻、推送站台 repo 的 PAT 失效。Binds: NFR8, NFR10, NFR19。Prevents: 這個專案真正的危險不是吵而是靜 —— 抓取被擋、PAT 過期、站台三天沒更新，而一切看起來都是綠的。現況 fetch.yml 刻意讓部分失敗不紅，等於告警機制存在但幾乎不會觸發。
- (decision) [ADOPTED] 來源層維持既有的 ports-and-adapters 形狀：src/types.ts 的 Source 介面為 port，src/sources/usj.ts 為 adapter。此為既有程式碼已具備的結構，脊椎只做批准，不重新發明。
- (decision) 呈現端不得對 usj.co.jp 的任何主機發出請求（包含但不限於圖片熱連結）。Binds: NFR3.1, NFR4。Prevents: 開出一條與使用者流量成正比、且完全不受 src/limiter.ts 管轄的對外請求通道 —— NFR4 的速率上限對它完全無效。註：FR25 的廣告版位是對第三方廣告主機的請求，不在此禁止範圍。
- (constraint) 依賴方向：渲染層 → 快照層 ← 協調層 → 節制層 → 取得層。渲染層只讀快照，永遠不得直接接觸來源；所有外送至來源的請求必須且只能經過 src/limiter.ts 這一個閘門。
- (constraint) H3（私有資料 repo 保留完整 git 歷史）順帶解除 O13：冷啟動是有損的（21 vs 23 個產品，EXP0070/EXP0082 靠 index.json 的 known 清單帶回），data/ 因此承載目錄取樣視窗看不到的狀態 —— 私有 repo 的完整歷史即為該狀態的備份，無須另設備份機制。
- (version) 查證 2026-08-21：Node.js 20 已於 2026-04-30 EOL，不再收安全修補。當期支援線為 22（Maintenance LTS）、24（Active LTS，EOL 2028-04-30）、26（Current）。本 repo 的 .node-version 釘 20.17.0 —— 正跑在無安全支援的執行環境上。脊椎釘 Node 24（Active LTS，最長跑道、最少變動；node:test 在 24 上亦更完整）。
- (event) 教練階段結束，全部 8 輪決策完成，進入 Finalize 蒸餾。
- (event) 【更正 — 前述現況勘查有誤】2026-08-21 直接以 GitHub API 與 curl 驗證：wisely051543/self-usj 為 public（private:false, visibility:public）、has_pages:true、建立於 2026-08-15；https://raw.githubusercontent.com/.../data/days.json 回 200；https://wisely051543.github.io/self-usj/ 回 200。站台已上線 6 天，抓取歷史已公開散佈。先前『無部署管線、站台不存在於網路上』的結論來自只看 git ls-files —— Pages 由 GitHub UI 以分支發佈設定，repo 內不留檔案，故未被偵測到。AD-5 與 AD-9 是建立在一個已被推翻的前提上。
- (version) 查證 2026-08-21（GitHub 官方計費文件）：私有 repo Actions 免費額度 Free 2,000 分/月、Pro 3,000、Team 3,000；公開 repo 使用標準 runner 免費不計量；計費以 job 為單位、不足一分鐘進位；Linux 2-core 超額 $0.006/分。以 PRD 實測暖機 436.8s 推算：cron */30 = 1,440 回合/月 × 約 9 分 ≈ 12,960 分，加建置 workflow 約 15,840 分，超額約 $83/月；NFR4 降並行度後約 $160/月。
- (version) 查證 2026-08-21（Vercel 官方 limits + 方案文件）：Hobby 方案明文限非商業個人用途，本產品有廣告變現（FR25）即屬商業；Hobby 的 Cron Jobs 最小頻率為每日一次，任何高於每日一次的 cron 運算式在部署時即失敗；Build Step 上限 45 分鐘；Pro 為 $20/月。
- (version) 查證 2026-08-21（GitHub Actions billing-and-usage 官方文件）：『GitHub Actions usage is free for standard GitHub-hosted runners in public repositories』；計費歸屬於 workflow 的 caller，即執行所在的 repo，而非它互動的 repo。
- (decision) 維持公開 repo 執行 workflow（Actions 免費不計量），另設一個純儲存的私有 repo 承載 data/ 與其歷史；workflow 以 PAT 讀寫私有 repo。Binds: 全部。Prevents: (a) 轉私有帶來的 $83~160/月 Actions 超額費，超過 PRD R1 估的廣告收入上限；(b) 單一公開 repo 下 raw.githubusercontent 永遠可取得 data/ —— Pages 不服務它也擋不住。取捨：公開的仍有程式碼、Actions 執行日誌與站台；src/sources/usj.ts 本就公開，抓取方法早非秘密。
- (constraint) 已散佈的六天抓取歷史（2026-08-15 起，255 個 commit）不可收回：fork、GHArchive、任何 clone 皆已複製。AD-5 只能是向前生效的規則，不得寫成一條可完全達成的狀態承諾。遷移須明確處置公開 repo 內既有的 data/ 歷史（保留或以歷史改寫移除，兩者皆不改變已散佈的事實）。
- (event) Reviewer Gate 完成：4 個視角、rubric 判 FAIL、合計 3 個 blocker 與多個 high。全文在 reviews/ 下四個檔。重大發現：(1) repo 實為公開且站台已上線，AD-5/AD-9 建立在錯誤前提上；(2) AD-12 的三態欄位在 days.json 現行結構上無法實作 —— 只保留 available:true 的列且無日期×票種軸，缺席格子無列可掛；(3) latestDate 有 10/31 個產品為空字串，620 個格子會整個翻面；(4) NFR15.2/15.3/15.4 整組漏掉；(5) AD-16 無『什麼都沒抓到』偵測器；(6) AD-15 三級用單一旗標表達不出，且 UI 停用會使 workflow_run 永不觸發、站台永遠進不了 L2；(7) AD-18 把 localizing skill 的意思寫反 —— 詞表的部分覆蓋是刻意的。
- (event) 脊椎重寫完成（v2）：AD 由 20 條增為 22 條，修掉 Reviewer Gate 的 3 個 blocker 與多數 high。主要變更：AD-5 改為公開 repo 執行 + 私有純儲存 repo 且向前生效；AD-4 移除 CONCURRENCY 靜態斷言、改為四值互鎖 + 冷啟動耗時斷言；AD-12 改為完整格網且禁止丟棄 available:false 列；AD-13 具名 latestDate 空字串缺口；AD-15 改為帶等級值的宣告檔且建置需獨立觸發入口；AD-16 新增合理性檢查；AD-18 改正兩張表的相反規則並納入 verified 來源狀態；新增 AD-8a（第三方腳本邊界）、AD-21（無消費者的抓取層須關閉）、AD-22（CI 是發佈前提）；補上 NFR15.2/15.3/15.4；Stack 移除 ts-node、更正 Cloudflare 排除理由。lint 0 findings。
- (event) spine finalized
